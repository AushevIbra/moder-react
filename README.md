## Модерирование тендеров и вакансий

Настройка:

### `yarn install || npm install` - Поставит все зависимости
Измените файл  `config/axios.js` - Поставьть нужный домен

### `yarn build`  - сгенерирует исходники в папку build